# Engineering Classification Probe

This temporary file verifies that a change under `.github/**` is classified as `engineering` without a Develop Loop label.

The pull request is a classification test and is not intended to merge.
