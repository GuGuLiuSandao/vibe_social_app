# Code Review 记录

## 1. Review 范围
- 黑名单系统 V1
- 协议、后端服务层、WebSocket 处理、前端关系页

## 2. 发现的问题
- [x] 需要避免在关系层仅做单向判断，聊天拦截必须按双向拉黑处理。
- [x] 拉黑后若不清理 follow 关系，好友/邀请候选列表会与业务语义冲突。
- [x] 前端不应把内部流程规则写入 README，对外说明与内部协作应分离。

## 3. 处理结果
- 服务层新增双向拉黑校验。
- 拉黑成功后自动清理双方 follow 关系。
- README 已保持对外展示定位，流程规则保留在 `AGENTS.md`。
- 已修正 follow 在黑名单下仍可建立的问题。
- 已移除会向目标方暴露“谁拉黑了我”的 block/unblock push。
- 前端已隐藏黑名单列表中的回关动作，并统一黑名单下的私聊/拉群禁用态。

## 4. 遗留事项
- 当前未处理群内共同成员的发言隔离。
- 当前未提供“谁拉黑了我”的显式提示，仅通过失败信息体现。
